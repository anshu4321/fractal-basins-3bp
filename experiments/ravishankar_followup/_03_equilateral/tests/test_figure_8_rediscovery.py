"""Sanity: the figure-8 orbit lives on the equilateral section.

After the basin runs (Task 3), we will search for IC cells with E ~ -1.287
and closure residual < 1e-9, and this test will verify the figure-8 is
rediscovered. Currently a placeholder -- passes trivially.
"""


def test_figure_8_placeholder():
    """True test implemented in Task 3 once basin exists."""
    assert True
